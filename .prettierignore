# Это новый файл, который нужно создать в корне вашего проекта. 
# Он говорит Prettier, какие файлы и папки не нужно форматировать. 
# Это очень важно, чтобы не трогать автоматически сгенерированные файлы.

# Папки с зависимостями
/node_modules

# Автоматически сгенерированные файлы блокировки пакетов
package-lock.json
yarn.lock

# Скомпилированные файлы (если они появятся в будущем)
/dist
/build